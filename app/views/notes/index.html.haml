.wrapper_with_padding
  #notes.clearfix
    - unless @notes.blank?
      - @notes.each do |note|
        %a{ href: (url_for [note])}
          .note
            %p.title= note.title
            %p.date= time_ago_in_words(note.created_at)
    - else
      %h2 Tambah Catatan
      %p Sepertinya kamu belum memiliki catatan, mengapa tidak tambah catatan sekarang!
      %button= link_to "Catatan Baru", new_note_path
